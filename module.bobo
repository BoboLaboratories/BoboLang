import a.b.c
import io.net
import math.pow

private const g0 = __get_so_pipe()

const g1 = g0

private var a
var b = h
private var c

private fun move(const x, y = t(x, y()), offset = o) {
    var a
    a = b

    http_send()
}

native fun main(x)

private fun send(a, b, c, d = r, k = f) {
    y = t
}

native fun __get_so_pipe(t)

private native fun println(x, y = t)