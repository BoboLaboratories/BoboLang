import a.b.c

module io {

    const g0 = __get_so_pipe();
    asd = 1

    fun move(const x, y, offset = o) {

    }

    native fun __get_so_pipe()

    native fun println()

    private native fun println()

}
